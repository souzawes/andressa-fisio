'use-client';
import { Montserrat } from "next/font/google";

const typography = Montserrat({
    weight: ['300', '400', '500', '700'],
    subsets: ['latin'],
    // display: 'swap',
});

export default typography;
