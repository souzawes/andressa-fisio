import Typography from '@mui/material/Typography';


const ListGroups = () => {
    return (
        <Typography variant='h1' color='secondary'>Lista de Grupos</Typography>
    );
}

export default ListGroups;